# Drop-in para /etc/systemd/journald.conf.d/journald-cerraduras.conf
# Limita el uso de disco del journal (planes por defecto de journald no
# establecen un tope explícito). Evita que el journal crezca sin control.
[Journal]
SystemMaxUse=500M
